sap.ui.define(['sap/uxap/BlockBase'], function (BlockBase) {
	"use strict";

	return BlockBase.extend("next.app.ORDR.pages.Summary", {
		metadata: {
			/* no additional views provided */
		}
	});
}, true);
